import { RepositoryItem} from './RepositoryItem'

export const RepositoryList= () =>{
return (
    <section>
    <h1>Respository list</h1>

    <ul>
        <RepositoryItem />
    </ul>
    </section>
)
}
